import React from 'react';

/**
 * CodeBookPage displays the actual codebook json. The page is composed
 * of a series of chapters, just like a book. Each chapter has its own
 * series of headings for navigation.
 *
 * Chapters are composed of various element types: Text, Markdown, CodeCell.
 * 
 * 
 */
export default class CodeBookPage extends React.Component {

};